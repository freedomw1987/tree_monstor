# Mock conflict-skill

Used to test merge-mode behavior when a skill name in the source tree
clashes with an existing user-owned skill in the target ~/.claude/skills/.

Installer must skip + warn, never overwrite.
