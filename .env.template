# Tree Monstor — Developer Profile Environment Variables
# macOS + Hermes 0.15.1 友善版 (2026-06-03)
#
# 使用方式:
#   cp adapters/hermes/.env.template .env
#   nano .env
#   填入下方的值(用 nano,不要 echo 進去 — 避免 token 進 shell history / LLM context)
#
# 重要:Hermes 啟動時會優先讀取 HERMES_HOME 底下的 .env
#       (對 developer profile 來說就是 ~/.hermes/profiles/developer/.env)
#       所以這個 .env 不會污染 default profile 的設定 ✅

# ──────────────────────────────────────────────────────────
# 必填:Discord bot token
# ──────────────────────────────────────────────────────────
# 這個 bot 專給 developer profile 用,**不可**跟 default profile 同一個
# (兩個 gateway 用同個 token 會互踢,後啟動的把先啟動的踢下線)
#
# 怎麼拿:
#   1. 到 Discord Developer Portal → Applications → New Application
#   2. Bot → Reset Token → 複製(這次只會顯示一次)
#   3. Bot → Privileged Gateway Intents → 開 Message Content Intent
#   4. OAuth2 → URL Generator → 勾 bot + applications.commands + Send Messages
#      → 用產生的 URL 邀請 bot 進你的 server
#   5. 把 token 貼到下面(在 nano 裡貼,不要用 echo)
#
# ⚠️ 這條 token 已經因為您之前的 Discord 訊息暴露在 LLM context 裡
#    請務必先去 Discord Developer Portal → Reset Token,然後用新 token
DISCORD_BOT_TOKEN=your_d...here

# ──────────────────────────────────────────────────────────
# 必填:OpenRouter API key
# ──────────────────────────────────────────────────────────
# Tree Monstor 的 subagent delegation 走 OpenRouter
# (MODEL_TIERING 的 simple / medium / complex 都會經過 OpenRouter)
#
# 怎麼拿:
#   1. 到 openrouter.ai → Sign in → Keys → Create Key
#   2. 給 $5 額度就夠用很久(subagent 主要是走 MiniMax-M3 或 gpt-4o-mini)
#   3. 把 key 貼到下面
#
# 注意:如果您以後想完全用 minimax 跑、不走 OpenRouter,
#      可以不填這個 key + 在 config.yaml 改 delegation.model 為
#      'minimax/MiniMax-M3'(但這樣會失去 model tiering 切換的彈性)
OPENROUTER_API_KEY=your_o...here

# ──────────────────────────────────────────────────────────
# 選填:MiniMax API key (簡單任務 subagent 用)
# ──────────────────────────────────────────────────────────
# 如果您已經在 default profile 設過 MiniMax,這條可以省略
# (Hermes 會從 default 的 .env fallback)
# 建議在這裡也設一份,避免哪天 default 拿掉時 developer profile 也跟著壞
MINIMAX_API_KEY=your_m...here

# ──────────────────────────────────────────────────────────
# 選填:Agent 設定覆寫
# ──────────────────────────────────────────────────────────
# Max iterations per session (預設 1000,長期任務可調高)
HERMES_MAX_ITERATIONS=1500

# Log level: debug / info / warn / error
LOG_LEVEL=info
