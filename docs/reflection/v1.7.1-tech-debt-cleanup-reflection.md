# v1.7.1 技術債清理 反省報告

**日期**：2026-09-25
**Backlog**：TMO-005（關閉）
**層級**：Module（v1.7 後續技術債清理）
**Commit**：`a45e498`

## 6 維度檢查

| # | 維度 | 結果 | 備註 |
| - | --- | --- | --- |
| 1 | UX/UI 一致性 | ✅ | 與 v1.7 拆目錄決策一致；test name 改英文不影響 bats UI 行為 |
| 2 | RWD 響應式設計 | N/A | 本次為 CLI / bats 變更，無 UI |
| 3 | 技術債 | ✅ | UTF-8 bug（homebrew bats）、`.agents/` 歷史殘留、TMO-005 全部清理；順手修 4 個 latent bash bug |
| 4 | 可維護性 | ✅ | test name 改為 ASCII 標準（CI 跨平台）；latent bugs 用最小 patch 修（${} 加 braces） |
| 5 | 測試覆蓋率 | ✅ | 從 0 executed → 209 tests 全綠（Gate 1/2/3 全 PASS） |
| 6 | 需求對齊 | ✅ | 用戶三次決策（方案 A：順手改完 7 個 + 修 latent bugs）與最終交付一致 |

## 改動清單

### 主要交付
- 12 個 bats 改中文 test name → 英文（137 個 test）
- 順手修 4 個 latent bash bugs（`${var}` 包裹 + setup REPO_ROOT）
- 5 處 `.agents/` 殘留清理（docs + CONTRIBUTING）
- TMO-005 關閉 + changelog v1.7.1 完整記錄
- 19 個檔案，+225/-149

### Gate 驗證
| Gate | 名稱 | 結果 |
| --- | --- | --- |
| Gate 1 | TDD | ✅ 15 bats / 209 tests pass |
| Gate 2 | lint | ✅ 9 scripts pass `bash -n` |
| Gate 3 | regression | ✅ == Gate 1 |
| Gate 4 | reviewer | ✅ PASS with Conditions（最終批准） |

## 過程檢討

### ✅ 做得好的
1. **V03 SOP 紀律**：SOP 修改必經 Reviewer 二審——成功抓到 2 次 P0 失誤（4 P0 + 1 P0），避免違背 AGENTS.md §1「誠實」原則
2. **用戶三次決策**：原始計畫太小（5 個 bats）→ Reviewer 抓漏擴大到 8 個 → Reviewer 再抓漏擴大到 15 個。每次都用 ask_user_question 確認，沒自作主張
3. **誠實記錄兩次 FAIL** 進 changelog v1.7.1：原始 audit 漏 7 個 bats / 89 個 test name 違背 §1「誠實」，在 changelog 中明確承認
4. **修法最小**：所有 latent bug 用最小 patch（`${var}` + 1 行 setup），無 scope creep
5. **修完後的 P2 瑕疵**（changelog 147→137、4 個 test name 太簡略）也順手修

### ⚠️ 待改進
1. **初次 audit 失誤**：以為 8 個 bats 已是全部，實際還有 7 個未被發現。原因：用 Python 統計但邏輯只看了「使用者提到的 8 個」沒全 repo grep
   - **Action**：未來 audit 一律全 repo grep，不憑印象
2. **regex 兩次錯誤破壞 ASCII 結構**：第一次 regex 太激進，移除了 `setup()`、`date.today()`、括號等 ASCII 內容
   - **Action**：未來 UTF-8 處理腳本必須「只動 @test 行的 "..." 內」絕不碰其他
3. **latent bugs 存在已久**：wiki-cleanup.sh:336、wiki-extract-media.sh:233/264 都是 v1.7 之前就已存在，只是被 homebrew bats UTF-8 bug 掩蓋
   - **Action**：未來新 bats / 改中文 test name 時，順便 grep 同類「中文+\$var」模式

### ❌ 重大失敗
無

## 後續 Action Items

| 優先級 | 項目 | 預估時間 | 狀態 |
| --- | --- | --- | --- |
| P2 | homebrew bats UTF-8 處理 bug（升級 bats 版本或 patch homebrew formula） | 1 小時 | 未開始（環境問題，非本次 scope） |
| P2 | 預防性加固：把另外 9 處 `中文 + $var` 模式全改 `${var}`（雖然目前未 trigger） | 30 分鐘 | 未開始 |
| P2 | `wiki-cleanup.bats` 12 個還未改英文的 test body 內中文 comment（不影響執行，但跨國開發可考慮） | 1 小時 | 未開始 |

## 結論

v1.7.1 是 v1.7 後續必要的技術債清理。從 0 executed（v1.7 之前）到 209 tests pass（v1.7.1），是真實的價值提升。

**主要學習**：
1. 兩次 Reviewer FAIL 證明 V03 SOP 紀律必要——沒 Reviewer 二審就會違背 §1「誠實」
2. 用戶三次決策證明 ask_user_question 機制有效——每次擴大 scope 都先確認
3. 誠實記錄錯誤（changelog 兩次 FAIL 段）建立信任

**狀態**：✅ 完成、已 commit `a45e498`、已 push origin/master