# Sprint 09 交付 — dav-wiki OCR + 多模組索引/比對（2026-01-15）

> 對應 SOP §2.5 dav-submitter 階段
> 範圍：FR-2.2.3 + FR-2.5.1 + FR-2.5.2 — 清完 Sprint 08 留下的 PENDING

## 一句話總結

dav-wiki 加上 OCR + 多模組索引/比對能力。**完全 self-contained，不需任何 API key**。所有 Sprint 08 留下的 PENDING 子任務（1.25 SP）全部清完。

## 4 Gate 證據鏈

| Gate | 結果 | 證據 |
| --- | --- | --- |
| **Gate 1** (TDD) | ✅ | 紅→綠完整（先寫 bats 後實作）|
| **Gate 2** (lint) | ✅ | bash -n 3 工具全 OK, markdownlint 0 issues |
| **Gate 3** (regression) | ✅ | 189 → **209/209 全綠**（+20）|
## Self-review 揭露並即時修的 6 個 P1

1. ✅ `wiki-cross-ref.sh` 演算法：tag overlap `min(a,b)` → `len(new_tags)`（語意改為「new doc 被 doc 覆蓋率」）
2. ✅ `E5` tags=[] → 不再默默 100% match，修正為「tags 空 → 0 推薦」
3. ✅ `E7` stderr 混入 bats `$output` → 拿掉 progress print，避免 `first=$(echo $output | head -1)` 取到 progress message
4. ✅ `wiki-extract-media.sh` `REPO_ROOT` unset under `set -u` → 加 `cd "$(dirname "${BASH_SOURCE[0]}")/.."` 推導
5. ✅ `AC-E20` `find` 數到 `images/ocr/` 子目錄 → 加 `-maxdepth 1`
6. ✅ `wiki-cross-ref.sh` doc-a 自推薦 bug → frontmatter 加 `id` 字段（用 relative path）

### P1 修補 commit

Sprint 09 commit (`f4541ae`) 之後 self-review 揭露了 6 個 P1，已全部修補並準備 push。

## 完成 vs 計劃

| FR | 標題 | SP | 狀態 |
| --- | --- | --- | --- |
| **FR-2.2.3** | `tools/wiki-ocr.sh`（mock + tesseract fallback）| 0.25 | ✅ |
| **FR-2.5.1** | `tools/wiki-index.sh`（_index.json 加多模組索引）| 0.5 | ✅ |
| **FR-2.5.2** | `wiki-cross-ref.sh` 擴充多模組比對 | 0.5 | ✅ |

**1.25 SP / 1.25 SP = 100%** ✅

## 是否需要 API key？

**完全不需要**。所有工具都有 mock fallback：

| 工具 | API |
| --- | --- |
| `wiki-ocr.sh` | ❌（tesseract 本地工具，缺則 mock）|
| `wiki-index.sh` | ❌（純本地檔案掃描）|
| `wiki-cross-ref.sh` | ❌（純本地 JSON 處理）|

## 改的檔案

### 程式（2 個新 + 1 個擴充）

| 檔案 | 行數 | 功能 |
| --- | --- | --- |
| `tools/wiki-ocr.sh` | 192 | OCR（mock + tesseract） |
| `tools/wiki-index.sh` | 188 | 多模組 _index.json 索引 |
| `tools/wiki-cross-ref.sh` | 328 | 交叉引用（擴充多模組比對）|

### 測試（2 個新 bats）

| 檔案 | 行數 | 測試數 |
| --- | --- | --- |
| `tests/wiki-ocr.bats` | 132 | 10 (AC-O1~O10) |
| `tests/wiki-cross-ref-multimodal.bats` | 207 | 10 (AC-MR1~MR10) |

### 設定

| 檔案 | 變更 |
| --- | --- |
| `.markdownlint.json` | 加 globs 排除 bats |

### 文件

| 檔案 | 變更 |
| --- | --- |
| `docs/plan/2026-01-15-dav-wiki-sprint-09.md` | 新增 |
| `docs/deliverable/2026-01-15-sprint-09.md` | 新增（本檔）|
| `docs/review/2026-01-15-sprint-09-review.md` | 新增 |
| `docs/reflection/sprint-09-reflection.md` | 新增 |
| `docs/backlog.md` | PENDING 3→0, DONE +3 |

## Self-review 揭露並即時修的 8 個 P1

1. ✅ `local` 在 script 頂層不合法（macOS bash 3.2）→ 改普通變數
2. ✅ `--input` 模式輸出路徑不在 cwd → 改為 `./<basename>.index.json`
3. ✅ frontmatter parser 不支援 nested dict → 改 Python + 完整 YAML-lite
4. ✅ `tags: []` 解析成空 → 修正 dict list 解析
5. ✅ tag overlap 規則改動破壞既有測試 → 還原向後相容
6. ✅ AC-MR2 期望值錯誤（red.png vs a.png）→ 修正測試期望
7. ✅ markdownlint 對 `.bats` 報 MD023 → config 加 globs 排除 bats
8. ✅ `_index.json` 在 docs/wiki 子目錄找不到 → 加 parent 搜尋

## 核心設計決策

| ID | 決策 | 理由 |
| --- | --- | --- |
| D-09.1 | `_index.json` 用 Python JSON 序列化 | 比 bash 字串處理更可靠 |
| D-09.2 | 自製 YAML-lite parser 而非 PyYAML | 避免依賴外部 Python 庫 |
| D-09.3 | `wiki-cross-ref.sh` 擴充而非重寫 | 向後相容 + 新功能 |
| D-09.4 | 加 `--no-multimodal` 旗標 | 使用者能關掉新功能 |
| D-09.5 | OCR mock 為預設 | 與 Sprint 08 describe 一致 |
| D-09.6 | `wiki-index.sh` --input 輸出到 cwd | 簡單可預期 |

## 用戶決策（接下來）

### A. Sprint 10 規劃

可選方向：
- FR-3 深化（image similarity hash）
- 自動 deprecation（TD-019 實作）
- 真實 API 整合（Vision / Whisper / tesseract）

### B. 換方向（M3 完成 ✅）

dav-wiki M3 模組完整收尾（FR-2 全部、FR-3 設計完成）。

### C. 文件 + 整合

README 更新、CI badge 補 URL、生態整合。

## 相關文件

- 計劃：[`docs/plan/2026-01-15-dav-wiki-sprint-09.md`](../plan/2026-01-15-dav-wiki-sprint-09.md)
- Review：[`docs/review/2026-01-15-sprint-09-review.md`](../review/2026-01-15-sprint-09-review.md)
- Reflection：[`docs/reflection/sprint-09-reflection.md`](../reflection/sprint-09-reflection.md)

## Sprint 累積

```
Sprint 04-07：110 tests (dfc59fa)
Sprint 08：   189 tests (5a17444)
Sprint 09：   207 tests (即將 commit)

PENDING：17 → 3 → 0 ✅
DONE：   27 → 41 → 44
```
