# TD-015 交付摘要 — Draft-07 `$comment` 三層註記

**日期**：2025-08-22
**Backlog ID**：TD-015（P3，1 SP）
**作者**：Agent
**狀態**：✅ 完成

---

## 摘要

把「gates.schema.json 為什麼用 Draft-07 而非原計劃 Draft 2020-12」的原因，**三層同步**寫進文件：
1. `gates.schema.json` 頂層 `$comment`（JSON Schema 標準欄位）
2. `gates.json` `description` 欄加說明
3. AGENTS.md §2.3 引用處加 1 句註腳

---

## 變更清單

| 檔案 | 改動 |
| --- | --- |
| `docs/sop/gates.schema.json` | +6 行 — 加 `$comment` 說明 Draft-07 原因 + 升級路徑（ajv API）|
| `docs/sop/gates.json` | `description` 欄加 Draft-07 說明（與 TD-015 ref） |
| `AGENTS.md` | §2.3 line 154 加 1 句註腳 |

---

## 驗收證據

- **Gate 1（TDD）**：豁免（純文件改動，符合 `gates.json` 的 `notes` 欄規範）
- **Gate 2（lint）** ✅：
  - `python3 -m json.tool`：兩個 JSON 0 error
  - `ajv validate -s ... -d ...`：仍 valid
- **Gate 3（regression）** ✅：bats 37/37 全綠
- **Gate 4（reviewer）** ✅：5 項手工 reviewer 全綠（schema `$comment` 完整、gates.json `description` 含說明、AGENTS.md 註腳、ajv 仍 valid、bats 全綠）

---

## 已知問題

無

---

## 下一步建議（V20）

- **驗收方式**：`python3 -c "import json; s=json.load(open('docs/sop/gates.schema.json')); print(s['$comment'][:80])"` 看 Draft-07 註解
- **預估時間**：< 5 分鐘
- **風險提示**：無（純文件改動，ajv 仍 valid）