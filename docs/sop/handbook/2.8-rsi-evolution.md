# §2.8 RSI Evolution — 遞歸自我改進 SOP

> **版本**：v1.0（2025-09-20 隨 Sprint 09 US-013 引入）
> **對應 Backlog**：US-013（1.5 SP）+ US-011/012/014/015/016/TD-022
> **對應 Gate 規範**：[`docs/sop/gates.json`](../../gates.json) Gate 5（RSI gate）
> **對應 V03 紀律**：AGENTS.md §1.5 V03（RSI 文檔修改必經 Reviewer 二審）

---

## 1. 用途

本章定義 **RSI（Recursive Self-Improvement）機制**的完整 SOP，包含：

- 為什麼需要 RSI
- 兩種模式（觀察 vs 聚合）的分離規則
- 完整流程圖（觀察 → 聚合 → 提案 → **Reviewer 二審** → 用戶審批 → 合併 → 同步）
- 4 條不可違反安全規則
- 安裝與部署指引

## 2. RSI 為何存在

### 2.1 問題

tree_monstor 已部署到多個專案，每個專案執行 SOP 時會遇到：

- 規範模糊 → Agent 卡住
- 規範不一致 → Agent 行為不可預測
- 用戶自己摸索 → 重複勞動

若沒有 RSI，**每個專案都會重複遇到同樣問題**，且「改進」只會在某個專案發生，**其他專案無感**。

### 2.2 解法

RSI 機制：

1. 觀察：每個裝了 tree_monstor 的專案在任務完成時**自動寫結構化觀察**（不動 SOP）
2. 聚合：用戶在**源 repo**打 `/reflect`，把所有專案的觀察聚合
3. 提案：聚合結果變成「候選 diff」
4. 二審：每個 diff 必經 **Reviewer subagent** 二審（V03 紀律）
5. 批准：用戶看到「diff + verdict」並呈後批准
6. 合併：套用 diff + git tag
7. 同步：`rsi-sync.sh` 把改完的 SOP 同步到所有已裝專案

## 3. 兩種模式分離（核心安全原則）

| 模式 | 環境 | 能力 | 觸發點 |
|---|---|---|---|
| **觀察模式**（observation） | 任何裝了 tree_monstor 的專案 | **只寫** observation JSON 到 `~/.tree-monstor/observations/` | 任務完成時自動 |
| **聚合模式**（aggregation） | **源 repo**（`tree_monstor/`） | 收集觀察 + 聚合 + 產出 diff 提案 | 用戶打 `/reflect` |

**為什麼分離**：裝在用戶專案裡的 agent 沒有權限動用戶的 SOP 源頭。改 SOP 只能由用戶**在源 repo**明確觸發。

詳細規範見 [`/skill:sop-evolver`](../../../.agents/skills/sop-evolver/SKILL.md)。

## 4. 完整流程圖

```
┌─────────────────────────────────────────────────────────────┐
│  RSI 完整流程：觀察 → 聚合 → 提案 → 二審 → 批准 → 合併 → 同步  │
└─────────────────────────────────────────────────────────────┘

[任何裝了 tree_monstor 的專案]
   ↓ 任務完成
[Gate 5 觸發] → sop-evolver skill（觀察模式）
   ↓ 寫 ~/.tree-monstor/observations/{project-id}/{YYYY-MM-DD}.json
   ↓ （觀察失敗不阻塞任務，observability 不在 critical path）
   ↓
[用戶 cd 回源 repo + 打 /reflect]
   ↓
[Gate 5] → sop-evolver skill（聚合模式）
   ↓ 收集所有專案的 observation
   ↓ 去重、排序、統計
   ↓ 產出 docs/sop/rsi-aggregated-{YYYY-MM-DD}.md
   ↓
[/propose] → rsi-propose.sh
   ↓ 讀聚合報告
   ↓ 對每個 Top N 信號產生「候選提案骨架」
   ↓ 寫 docs/sop/rsi-proposals/{date}-{NN}.md
   ↓ （不直接生成 diff，避免 agent 自動修改）
   ↓
[Reviewer 二審] ⚠️ V03 紀律（必經）
   ↓ dev-checker-loop (Reviewer subagent)
   ↓ 風險分級 🟢/🟡/🔴 + 跨 SOP 一致性檢查 + 修改建議
   ↓ 寫 docs/sop/rsi-reviewer-verdict-{date}.md
   ↓
[用戶審批]
   ↓ 用戶看到「diff + verdict」兩者並呈
   ↓ 用戶批准 / 退回 / 要求改架構
   ↓ （可明確說「跳過 Reviewer」明示豁免）
   ↓
[套用 diff + git tag rsi-v{YYYYMMDD}-{NN}]
   ↓
[更新 docs/sop/rsi-log.md]
   ↓
[Gate 5 通過] 任務完成
   ↓
[rsi-sync.sh] → 同步 SOP 到所有已裝專案
   ↓ 透過 symlink + rsi-overlay.sh
   ↓ 不覆蓋用戶本地 override（sync 規則見 §6.3）
   ↓
[觀察下一個任務 → 循環改進]
```

## 5. 觀察記錄存放位置

```
~/.tree-monstor/observations/{project-id}/{YYYY-MM-DD}.json
```

- `project-id` = `SHA256(安裝路徑).substring(0,8)`（**絕不存明文路徑**）
- 每個 task 寫一個 JSON 檔
- 觀察失敗**不阻塞**任務（log warning，跳過寫入）

### 5.1 觀察 JSON Schema（白名單 + 黑名單雙重保護）

**白名單欄位**（多餘欄位 → ajv reject）：

| 欄位 | 型別 | 說明 |
|---|---|---|
| `task_id` | UUID | 任務 ID |
| `project_id` | string | `SHA256(path).substring(0,8)` |
| `timestamp` | ISO-8601 | 觀察時間 |
| `gate_results` | object | 5 Gate 的 pass/fail（Gate 1-5，含 Gate 5 RSI gate） |
| `skills_used` | array | 使用的 skill 名 |
| `failure_signals` | array | 失敗信號 `{gate, type, count}` |
| `duration_seconds` | integer | 任務耗時 |

**黑名單欄位**（被拒絕）：

- ❌ `raw_conversation`（raw 對話內容）
- ❌ `code_snippets`（程式碼片段）
- ❌ `file_paths`（檔案絕對路徑明文）
- ❌ `env_values`（環境變數值）
- ❌ `git_messages`（git commit message 內容）

詳見 [`/skill:sop-evolver/observation.md`](../../../.agents/skills/sop-evolver/observation.md)。

## 6. 4 條不可違反安全規則

### 6.1 規則 1：觀察/改動分離

| 允許 | 禁止 |
|---|---|
| 任何裝了 tree_monstor 的專案都**可寫** observation | 任何裝了 tree_monstor 的專案都**不可改** SOP/AGENTS.md/gates.json/handbook/skill |

### 6.2 規則 2：匿名化

| 允許 | 禁止 |
|---|---|
| 存 `project_id` = `SHA256(path)[:8]` | 存**明文路徑**、**明文專案名**、**明文程式碼** |
| 存結構化信號（gate 結果、失敗類型、頻率） | 存 **raw text**、**絕對路徑**、**環境變數值** |

### 6.3 規則 3：Reviewer 二審必經

所有 SOP / AGENTS.md / gates.json / handbook / skill 修改提案：

1. **必先經** `dev-checker-loop` Reviewer subagent 二審
2. Reviewer 必附風險分級（🟢/🟡/🔴）+ 跨 SOP 一致性檢查 + 修改建議
3. 用戶收到「diff + verdict」兩者並呈
4. 用戶可**明確說「跳過 Reviewer」**直接批准（明示豁免）

### 6.4 規則 4：一鍵回滾

每次合併 diff 必自動寫 git tag：

```
rsi-v{YYYYMMDD}-{NN}
```

回滾指令：

```bash
./tools/rsi-rollback.sh rsi-v{YYYYMMDD}-{NN}
```

**特性**：回滾時間 < 5 秒（純 git checkout），觀察記錄**不刪**。

## 7. 安裝與部署

### 7.1 安裝旗標

```bash
./install.sh --enable-rsi   # 預設：部署 sop-evolver skill + 建觀察目錄
./install.sh --disable-rsi  # 不部署 sop-evolver，移除觀察目錄
./install.sh --uninstall    # 完全清理
```

### 7.2 觀察目錄建立

```bash
mkdir -p ~/.tree-monstor/observations/
```

### 7.3 驗證安裝

```bash
# 確認 skill 已部署
ls ~/.pi/sop/skills/sop-evolver/SKILL.md  # 或對應安裝路徑

# 確認觀察目錄可寫
echo '{"test": true}' > ~/.tree-monstor/observations/test.json
rm ~/.tree-monstor/observations/test.json
```

### 7.4 Pi Extension auto-observe hook（v1.1+）

**為什麼需要**：原本 Gate 5 觀察依賴 agent 自律（主動寫 observation JSON），容易遺漏。v1.1+ 加入 Pi Extension，在 LLM 呼叫 `TaskUpdate(status="completed")` 時**自動**寫觀察。

**部署內容**（由 `install.sh --enable-rsi` 自動安裝到 `~/.pi/agent/extensions/`）：

| 檔案 | 用途 |
|---|---|
| `auto-observe.ts` | Pi Extension hook `tool_call(TaskUpdate)` → 叫用 observe-pi-task.sh |
| `observe-pi-task.sh` | 讀 `.pi/tasks/tasks-{sessionId}.json` → 轉 observation → atomic write 到 `~/.tree-monstor/observations/{project_id}/{YYYY-MM-DD}.json` |

**設計原則**：

1. **不依賴 agent 自律**：Extension 自動攔截 tool_call，無需 agent 記得寫 observation
2. **不阻塞任務**：使用 `execFileAsync` fire-and-forget，觀察失敗不影響 TaskUpdate 結果
3. **降噪**：每 session 只 notify 一次成功；失敗每次 warning
4. **冪等去重**：`.observed-tasks` 檔記錄已觀察的 task_id，重複 TaskUpdate 不會重複寫
5. **原子寫入**：temp file + `mv`（POSIX atomic），避免 race condition
6. **Schema 守門**：白名單 7+1 欄位 + 黑名單（不存 raw_conversation / file_paths / code_snippets）

**驗證安裝**：

```bash
# 1. 確認 Extension 跟 Script 已部署（兩個檔同目錄）
ls -la ~/.pi/agent/extensions/
# 預期看到：auto-observe.ts  +  observe-pi-task.sh (executable)

# 2. TypeScript 靜態型別檢查
cd /path/to/tree_monstor
npx -y -p typescript@5.6 tsc --noEmit -p extensions/tsconfig.json

# 3. 跑觀察測試
bats tests/observe-pi-task.bats
# 預期 15 ok

# 4. 手動 E2E 測試（可選）：在 pi session 跑 TaskUpdate(completed)，驗證 observation 寫入
ls ~/.tree-monstor/observations/$(shasum -a 256 < <(pwd) | cut -c1-8)/  # 今日日期.json
```

**Claude Code 不適用**：無 Pi Extension 支援。Claude 用戶必須透過 sop-evolver skill 手動觸發，或等未來 Claude hook 等價機制。

**對應 Reviewer V03 verdict**：`docs/sop/rsi-reviewer-verdict-2026-09-22-auto-observe-v2.md` APPROVE_WITH_NITS。

**對應 BACKLOG US-016 擴展錨點**：`docs/backlog.md` US-016「自動觀察觸發」。

## 8. 故障排除

### 8.0 auto-observe hook（v1.1+）相關

| 問題 | 排查 |
|---|---|
| Extension 未載入 | 確認 `~/.pi/agent/extensions/auto-observe.ts` 存在；重啟 pi session |
| 觀察寫入失敗 | 看 pi UI notification（會顯示 `RSI observation failed: ...`）；檢查 `~/.tree-monstor/observations/` 權限 |
| 重複觀察 | `cat ~/.tree-monstor/observations/{project_id}/.observed-tasks` 確認 task_id 是否已記錄 |
| taskScope=project 不寫入 | v1 只支援 `session` scope；改 `taskScope: "session"` 或手動觀察 |
| 在 Claude Code 下完全沒有觸發 | 預期行為：Claude 無 Pi Extension 支援；改用 sop-evolver skill 主路徑 |
| `tsc --noEmit` 報錯 | 用 stub `extensions/types/pi-coding-agent.d.ts`（已包含）或裝本機 `@types/node` + 讓 tsc 找到全域 pi-coding-agent |

### 8.1 觀察記錄沒寫

| 可能原因 | 檢查 |
|---|---|
| observation schema 驗證失敗 | 看 `~/.tree-monstor/logs/observation-{date}.log` |
| 觀察目錄不存在 | `mkdir -p ~/.tree-monstor/observations/` |
| 寫入權限不足 | `chmod 700 ~/.tree-monstor/observations/` |

### 8.2 聚合報告空

| 可能原因 | 檢查 |
|---|---|
| 沒觀察資料 | `ls ~/.tree-monstor/observations/`，確認有 JSON 檔 |
| 觀察時間範圍不對 | 預設聚合最近 30 天，可在 `rsi-aggregate.sh` 改 `--days` |

### 8.3 Reviewer 二審失敗（🔴）

| 處理 | 動作 |
|---|---|
| 提案標記「不可合併」 | 等待用戶決定 |
| 用戶退回 | 提案保留 7 天後封存到 `_archive/` |
| 修改提案 | 重新跑 `/propose` + Reviewer |

## 9. 量化指標（rsi-metrics.sh）

| 指標 | 期望值 | 紅線 |
|---|---|---|
| 觀察失敗率（schema reject） | < 5% | > 20% |
| 違規事件數（incident log） | 0 | > 0 |
| Reviewer 跳過率 | < 10% | > 30% |
| 回滾平均時間 | < 10 秒 | > 60 秒 |
| 規範違規次數 | 下降趨勢 | 持續上升 |
| 跨專案觀察分佈 | ≥ 3 個專案 | < 3 個專案 |

## 10. 與其他章節關係

| 章節 | 關係 |
|---|---|
| §2.1 規劃 | RSI 是 §2.4 反省 → §2.1 規劃的自動化閉環 |
| §2.3 執行 | Gate 5（RSI gate）由 sop-evolver skill 觸發 |
| §2.5 提交 | diff 合併後必更新 rsi-log.md + 同步到其他專案 |
| §2.7 違規 | 違反 4 條規則 → incident log → V03 二審 |

## 11. 參考資料

- 設計文件：[`docs/plan/2025-09-20-rsi-mechanism.md`](../../plan/2025-09-20-rsi-mechanism.md)
- PRD：[`docs/prd/04-self-evolution.md`](../../prd/04-self-evolution.md)
- 系統設計：[`docs/system-design.md`](../../system-design.md) §4 M4
- 觀察規範：[`/skill:sop-evolver/observation.md`](../../../.agents/skills/sop-evolver/observation.md)
- 聚合規範：[`/skill:sop-evolver/aggregator.md`](../../../.agents/skills/sop-evolver/aggregator.md)
- 提案模板：[`/skill:sop-evolver/proposer.md`](../../../.agents/skills/sop-evolver/proposer.md)
- 安全規則：[`/skill:sop-evolver/safety.md`](../../../.agents/skills/sop-evolver/safety.md)

---

## §2.8 RSI Evolution SOP — 完成（2025-09-20）
